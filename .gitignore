# Swift Package Manager
.build/
.swiftpm/

# Built app bundle (regenerate with scripts/build-app.sh)
dist/

# Xcode
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/
*.xcuserstate

# AppStore/ is an xcodegen project: project.yml is the source of truth, the .xcodeproj and
# entitlements file are generated from it — regenerate with AppStore/generate.sh.
AppStore/TranslateStore.xcodeproj/
AppStore/TranslateStore.entitlements

# macOS
.DS_Store

# Claude Code local settings (machine-specific permissions, not shared project config)
.claude/

# Unrelated scratch Xcode project accidentally created in this folder — not part of this app
Mew/
